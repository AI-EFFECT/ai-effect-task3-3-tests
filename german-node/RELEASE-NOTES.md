# Release Notes — v2.0

This is the first clean Germany-node baseline release. It supersedes the local trial packages v0.1 to v1.3.

It includes only scripts that were successfully exercised on a Windows PowerShell 5.1 environment against the frozen AI-EFFECT baseline `486ccc702bb2afca79fb7d31258d62ee0f94dbfc`.

The package records, rather than hides, the VILLASnode dependency blocker and two confirmed findings: the empty packaged generator input and persistent workflow service keys in Redis.
